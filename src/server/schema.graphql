  type Query {
    user(id: ID!): User
    sensor(id: ID!): Sensor
    sensors: [Sensor]
    value(id: ID!): Value
    values: [Value]
    users: [User]
  }

  type User {
    id: ID!
    userName: String
    email: String
    employeeId: Int 
    password: String
    token: Boolean
  }

  type Sensor {
    id: ID!
    name: String
    location: String
    readings: [Value]
  }

  type Value {
    id: ID!
    time: String
    sensor: Sensor
    data: Int
  }

  # type Mutation {
  #   addUser(userName: String, email: String, employeeId: Int, id: Int, password: String token: Boolean ): Users
  # }
