import React, { useState } from 'react';
import propTypes from 'prop-types';



const AlertsHandler = () => {

  return (
    <button style={{ width: '20%' }} type="button">Submit</button>
  );
};

AlertsHandler.propTypes = {
};

AlertsHandler.defaultProps = {
};

export default AlertsHandler;
